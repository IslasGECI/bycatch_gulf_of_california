version: "3.1"
services:
  islasgeci:
    build:
      context: .
      dockerfile: Dockerfile
    image: islasgeci/bycatch_gulf_of_california:latest
    command: bash
    volumes:
      - $PWD:/workdir
